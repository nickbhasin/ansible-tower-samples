- name: Merge provided configuration with device configuration
  cisco.nxos.nxos_vrf:
    name: temp
    description: Test
    state: merged
  register: vrf_output

- name: Print VRF module response
  debug:
    var: vrf_output
